// square.js

// Step 1: 二乗を計算して戻り値として返す関数を定義
function square(num) {
  return num * num; // 引数 num を二乗して返す
}

// Step 2: 関数を呼び出して結果をコンソールに出力
const result = square(10); // 引数に10を渡す
console.log(result);        // コンソールに100が出力される
